# Bot 语音沟通后端 - 依赖列表

# 核心依赖
requests>=2.28.0

# Web 框架 (可选，用于启动 REST API 服务)
flask>=2.3.0

# CSRF 防护 (可选，flask-wtf 提供 CSRF 保护)
# flask-wtf>=1.2.0

# 异步支持 (可选)
aiohttp>=3.8.0

# 测试框架 (可选)
pytest>=7.0.0

# 环境变量管理 (可选)
python-dotenv>=1.0.0

# 日志管理 (可选)
loguru>=0.6.0

# JWT 认证 (可选，用于 JWT Token 认证)
PyJWT>=2.8.0
